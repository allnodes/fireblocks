import { stakeManagerAbi } from './stake-manager-abi';
import { tokenAbi } from './token-abi';
import { validatorShareAbi } from './validator-share-abi';

/**
 *
 */
export const abi = {
  token: tokenAbi,
  stakeManager: stakeManagerAbi,
  validatorShare: validatorShareAbi,
};
